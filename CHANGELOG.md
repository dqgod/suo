# 更新记录

## v0.1.2（2026-09-11）

- 修复 Windows 捕获 Python、PowerShell 或可执行脚本输出时可能出现的中文乱码：Python 明确输出 UTF-8，其他纯文本在严格 UTF-8 解码失败后回退当前 Windows 系统代码页。
- 支持脚本结果中的内部换行；启动器标题最多展示两行，复制结果时保留原有内部换行，首尾空白仍按既有协议去除。
- 增加 UTF-8 中文、GBK/CP936 回退、多行标题和复制内容的回归测试，不改变 argv 参数传递、超时、取消和 1 MB 输出上限。
- 将自带脚本作为只读模板初始化到平台用户脚本目录；升级和重装不再覆盖已编辑脚本。配置协议升级到 v16，旧版默认 `ts` 路径自动迁移，其他自定义路径保持不变。
- 纯文本脚本支持以 `SUO_RESULT:` 明确标记返回行，未标记的 stdout 日志会被忽略；无前缀旧脚本保持原有完整 stdout 行为。模板与时间戳示例已改用统一的 `emit_result()`。
- 修复 Windows“在文件夹中显示”可能只打开 Explorer 而未定位脚本的问题，改用系统 Shell 接口直接打开父目录并选中文件；macOS 继续使用 Finder `open -R`。
- 修复 Windows 用户脚本相对路径包含 `/` 时 Shell 无法解析的问题；传入原生定位接口前以 UTF-16 无损统一为 `\`，支持中文路径和混合分隔符。
- 脚本单结果协议新增 `text`、`image`、`qrcode` 类型：受限 PNG/JPEG/WebP 可直接显示，二维码由 Suo 本地生成；二维码显式声明 UTF-8、限制 500 个 UTF-8 字节并在生成的 PNG 中保持至少 4 px/模块；旧 `SUO_RESULT:` 与无前缀 stdout 保持兼容。
- 配置协议升级到 v17；关键字未被占用时新增默认 `qr` 命令和只创建不覆盖的 `qr.py` 模板。脚本/网络搜索删除改为应用内二次确认框，避免单击误删。
- 搜索结果中的所有脚本图片统一显示为最大 240 px、随窗口高度继续缩小的完整缩略图，避免遮挡说明和底部操作栏；后端图片质量与二维码原图尺寸不受影响。`script_template.py` 同时补充文字、图片与二维码三类互斥返回 helper 和可复制示例。
- 完善 `timestamp.py` 示例的参数校验：半小时/非整点时区显示完整分钟，非法时区和多余参数返回可读错误，不再产生 traceback 或静默忽略参数。
