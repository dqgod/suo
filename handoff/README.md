# Cross-machine handoff

本目录保存 Suo 在 Windows 与 macOS 之间切换开发机、编译环境或 Codex agent 时需要继承的阶段状态。长期有效的仓库规则仍在根目录 `AGENTS.md`；这里记录会随硬件验证推进而变化的清单、证据和已知坑。

最后更新：2026-08-10，分支 `dev`。

## 当前状态

| 范围 | 状态 | 下一步 |
| --- | --- | --- |
| macOS Apple Silicon 基线 | **已完成** | 后续平台改动按 `MACOS.md` 做回归；仍保留少量必须肉眼检查的项目。 |
| 2026-08-09 macOS `dev` 增量 | **已完成** | 配置已演进到 v10；几何设置、选中行、菜单栏模板图标和 Dock 显隐均已验证。 |
| 2026-08-10 macOS v11 增量 | **已完成** | 原生 arm64 构建、快捷键录制/持久化、Bundle 本地化别名和固定 URL 直达均已实机验证；系统级快捷键注入仍保留人工回归。 |
| 跨平台配置文件位置 | **macOS 已完成；Windows 待验证** | macOS 已通过 Finder、原生目录选择器、迁移/恢复和两次重启验证；Windows 按 `WINDOWS.md` 验证 Explorer、含空格/中文目录及失败回滚。 |
| Windows v10 基线 | **已完成** | x64 构建、69 项测试及人工验收通过；搜索窗口不占任务栏，设置窗口保留彩色任务栏入口。 |
| Windows v11 增量 | **待验证** | 按 `WINDOWS.md` 验证快捷键录制/冲突回滚、v10→v11 迁移和固定 URL；不得改弱现有 Windows 平台路径。 |

## 文件导航

- [`WINDOWS.md`](WINDOWS.md)：下一位 Windows agent 的直接执行清单、预期结果和回报模板。
- [`MACOS.md`](MACOS.md)：已完成的 Apple Silicon 验证、仍需人工观察的项目和未来回归入口。
- [`CROSS_PLATFORM.md`](CROSS_PLATFORM.md)：跨系统切换时已经踩过的工具链、配置迁移和平台隔离问题。

## 维护规则

1. 开始平台相关工作前先读根目录 `AGENTS.md`，再读本文件和目标平台文件。
2. 项目状态只能标记为“待验证”“阻塞”或“已完成”；完成后勾选并记录命令/真实场景，不保留含糊的过期待办。
3. 阻塞项必须保留完整命令、退出码、stderr、系统版本、架构和必要截图。
4. 配置迁移测试必须先备份真实 `config.json` 与 `.bak`，结束后验证原始文件或预期迁移结果，禁止把测试值留给用户。
5. 不在本目录保存密钥、token、代理凭据、用户查询内容或构建产物。
6. `README.md` 只维护状态索引；细节放入对应平台文件，避免重新把阶段日志堆回 `AGENTS.md`。

## 完成交接的判定

一次平台交接只有在以下条件同时满足时才算完成：

- 当前分支和远端状态明确；
- 依赖、前端、Rust 测试和目标平台原生构建通过；
- 受影响的真实应用行为在对应硬件上验证；
- 配置和用户数据已恢复或完成可审计迁移；
- 本目录的状态与主 `README.md` 不冲突。
